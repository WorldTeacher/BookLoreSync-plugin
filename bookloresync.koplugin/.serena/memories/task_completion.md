# Task Completion Checklist

When finishing a task on this project:

1. **Run tests** (from `/home/alexander/GitHub/booklore-koreader-plugin/`):
   ```bash
   bash run_tests.sh
   ```
   All 4 suites must pass: `test_api_client.lua`, `test_main.lua`, `test_updater.lua`, `test_cfi.lua`

2. **No linter or formatter** is configured - no additional steps needed.

3. **Do NOT manually edit** `plugin_version.lua` - it is auto-generated by `generate-version.sh`.

4. **Verify KOReader API compatibility** - plugin uses KOReader internal modules (datastorage, UIManager, etc.); changes must remain compatible.
